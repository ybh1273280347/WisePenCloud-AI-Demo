# Web Search 彻底重构执行文档：默认并发多搜 + Normal / Deep 两种模式

当前 `WebSearchTool` 已经承载了大量参数与分支：`query/queries`、`fetch_top_pages`、`fetched_page_max_chars`、`fetch_page_timeout_seconds`、`allow_paid_fallback`、`dedupe_domains`、`include_domains`、`exclude_domains`、`time_range` 等，确实已经到了需要**彻底收口**的阶段。

这次重构目标很明确：

```text
默认就是并发多搜。
只分 normal 和 deep 两种模式。
删除高级参数。
删除单 query 精搜分支。
删除幽灵功能。
Tavily 不走。
```

---

# 1. 最终设计

## 1.1 最终只有两种模式

```text
normal:
    多 query 并发搜索
    返回搜索结果摘要

deep:
    多 query 并发搜索
    返回搜索结果摘要
    额外读取 Top 页面正文片段
```

不再有：

```text
precise
broad
fetch_top_pages
include_page_content
freshness_required
allow_paid_fallback
include_domains
exclude_domains
time_range
```

---

# 2. 最终 Tool 参数

最终 `_TOOL_SCHEMA` 只保留：

```text
queries
mode
with_images
```

其中：

| 参数            | 类型              |      默认 | 作用            |                     |
| ------------- | --------------- | ------: | ------------- | ------------------- |
| `queries`     | `array[string]` |      必填 | 1-4 条搜索 query |                     |
| `mode`        | `"normal"       | "deep"` | `"normal"`    | 普通搜索 or Deep Search |
| `with_images` | `boolean`       | `false` | 是否附带图片结果      |                     |

---

# 3. 为什么删除 `query`

之前：

```text
query  = 单 query 精搜
queries = 多 query 广搜
```

现在原则改成：

```text
web_search 默认就是并发多搜。
```

所以不再需要两个入口。

简单问题也传一条 query：

```json
{
  "queries": ["Python asyncio gather"],
  "mode": "normal"
}
```

复杂问题传多条 query：

```json
{
  "queries": [
    "Python asyncio gather vs wait",
    "site:docs.python.org asyncio gather wait",
    "asyncio return_when FIRST_COMPLETED"
  ],
  "mode": "deep"
}
```

这样 Tool 语义最清楚：

```text
永远是 queries。
一条也是 queries。
多条也是 queries。
```

---

# 4. 最终内部固定策略

在 `web_search_tool.py` 中定义：

```python
SEARCH_QUERY_LIMIT = 4

RESULTS_PER_QUERY = 8
FINAL_RESULT_LIMIT = 20

ALLOW_PAID_FALLBACK = False

DEDUPE_DOMAINS = True
MAX_PER_DOMAIN = 2

PAGE_CONTENT_LIMIT = 8
PAGE_CONTENT_CONCURRENCY = 5
PAGE_CONTENT_TIMEOUT_SECONDS = 15.0
PAGE_CONTENT_WINDOW_CHARS = 3000
```

说明：

```text
queries 最多 4 条。
每条 query 默认取 8 个结果。
合并去重后最多保留 20 个结果。
Deep Search 默认读取 Top 8 个页面正文片段。
页面正文读取最大并发 5。
单页面读取超时 15 秒。
每个页面正文窗口 3000 字符。
Tavily 永远不由 WebSearchTool 触发。
```

---

# 5. 最终 Tool Schema

替换 `_TOOL_SCHEMA` 为：

```python
_TOOL_SCHEMA = {
    "type": "object",
    "properties": {
        "queries": {
            "type": "array",
            "items": {"type": "string"},
            "minItems": 1,
            "maxItems": 4,
            "description": (
                "One to four focused search-engine-style queries. "
                "Use one query for simple lookup. Use 2-4 queries for complex questions, "
                "technical research, comparisons, debugging, or source-backed answers."
            ),
        },
        "mode": {
            "type": "string",
            "enum": ["normal", "deep"],
            "description": (
                "Search mode. normal returns search result titles, URLs, and snippets. "
                "deep also reads page content snippets from top-ranked result URLs."
            ),
            "default": "normal",
        },
        "with_images": {
            "type": "boolean",
            "description": (
                "Whether to include relevant image results. "
                "Use for pictures, photos, locations, people, animals, products, "
                "UI screenshots, or visual references."
            ),
            "default": False,
        },
    },
    "required": ["queries"],
}
```

删除所有旧参数：

```text
query
max_results
final_max_results
fetch_top_pages
fetch_top_pages_limit
include_page_content
page_content_limit
freshness_required
fetch_page_timeout_seconds
fetched_page_max_chars
allow_paid_fallback
dedupe_domains
max_per_domain
include_domains
exclude_domains
time_range
```

---

# 6. Tool Description 重写

替换 `_TOOL_DESCRIPTION` 为：

```python
_TOOL_DESCRIPTION = (
    "Searches the web with concurrent multi-query search. "
    "The tool always accepts queries as a list. Use one query for simple lookup and "
    "2-4 focused queries for complex research, comparisons, debugging, or source-backed answers.\n\n"
    "Modes:\n"
    "1) normal: concurrent web search, returning titles, URLs, snippets, and optional images.\n"
    "2) deep: concurrent web search plus page content snippets from top-ranked result URLs.\n\n"
    "Use normal mode when search result snippets are enough. "
    "Use deep mode when the answer needs stronger evidence from page content.\n\n"
    "Query generation guidance:\n"
    "- Keep each query short and search-engine-style.\n"
    "- For technical questions, include one official-documentation query when possible, such as site:docs.python.org.\n"
    "- For debugging, include exact error keywords and library/framework names.\n"
    "- For comparisons, use focused A vs B queries plus official documentation queries.\n\n"
    "Cost policy: Tavily paid fallback is disabled for this tool."
)
```

---

# 7. WebSearchTool 重构

文件：

```text
chat/application/tools/web_search_tool.py
```

## 7.1 删除旧参数读取

删除：

```python
query = ...
max_results = ...
final_max_results = ...
fetch_top_pages = ...
fetch_top_pages_limit = ...
include_page_content = ...
page_content_limit = ...
freshness_required = ...
fetch_page_timeout_seconds = ...
fetched_page_max_chars = ...
allow_paid_fallback = ...
dedupe_domains = ...
max_per_domain = ...
include_domains = ...
exclude_domains = ...
time_range = ...
```

改成：

```python
notes: List[str] = []

queries = normalize_queries(
    kwargs.get("queries", []),
    limit=SEARCH_QUERY_LIMIT,
    notes=notes,
)

if not queries:
    return "[Tool Error] Missing required queries parameter."

mode = kwargs.get("mode", "normal")
with_images = kwargs.get("with_images", False)
```

不做二次类型校验。
不做宽容 clamp。
信任 Tool Schema。

---

## 7.2 site: 自动关闭 domain dedupe

```python
dedupe_domains = DEDUPE_DOMAINS

if has_site_operator(queries):
    dedupe_domains = False
    add_note(notes, "Domain dedupe disabled because a site: operator was detected.")
```

注意：不再判断 `len(queries) > 1`。

因为现在所有搜索都是 `queries`，即使只有一条：

```text
site:docs.python.org asyncio gather
```

也应该关闭 domain dedupe。

---

## 7.3 永远走 search_many

删除单 query 分支。

不再：

```python
if len(queries) == 1:
    self._coordinator.search(...)
else:
    self._coordinator.search_many(...)
```

统一：

```python
response = await self._coordinator.search_many(
    queries=queries,
    max_results_per_query=RESULTS_PER_QUERY,
    final_max_results=FINAL_RESULT_LIMIT,
    with_images=with_images,
    dedupe_domains=dedupe_domains,
    max_per_domain=MAX_PER_DOMAIN,
    notes=notes,
)
```

这就是“默认并发多搜”的核心。

即使 `queries` 只有一条，也走 `search_many()`。
这样链路统一，没有精搜 / 广搜分裂。

---

## 7.4 Deep Search 分支

```python
page_contents: List[str] = []

if mode == "deep":
    page_contents = await self._read_page_contents(
        response,
        session_id=session_id,
        limit=PAGE_CONTENT_LIMIT,
        notes=notes,
    )

    if not page_contents:
        add_note(notes, "Deep search was requested but no page content was read.")
```

---

## 7.5 最终 execute 主体形态

目标结构：

```python
async def execute(self, context: Dict[str, Any], **kwargs: Any) -> str:
    session_id: Optional[str] = context.get("session_id")
    if not session_id:
        return "[Tool Error] Missing session_id in execution context."

    notes: List[str] = []

    queries = normalize_queries(
        kwargs.get("queries", []),
        limit=SEARCH_QUERY_LIMIT,
        notes=notes,
    )
    if not queries:
        return "[Tool Error] Missing required queries parameter."

    mode = kwargs.get("mode", "normal")
    with_images = kwargs.get("with_images", False)

    dedupe_domains = DEDUPE_DOMAINS
    if has_site_operator(queries):
        dedupe_domains = False
        add_note(notes, "Domain dedupe disabled because a site: operator was detected.")

    try:
        response = await self._coordinator.search_many(
            queries=queries,
            max_results_per_query=RESULTS_PER_QUERY,
            final_max_results=FINAL_RESULT_LIMIT,
            with_images=with_images,
            dedupe_domains=dedupe_domains,
            max_per_domain=MAX_PER_DOMAIN,
            notes=notes,
        )
    except Exception as e:
        log_fail(
            "联网搜索工具",
            e,
            session_id=session_id,
            queries=queries,
            mode=mode,
            with_images=with_images,
        )
        return "[Tool Error] Unexpected error while searching the web."

    page_contents: List[str] = []

    if mode == "deep":
        page_contents = await self._read_page_contents(
            response,
            session_id=session_id,
            limit=PAGE_CONTENT_LIMIT,
            notes=notes,
        )

        if not page_contents:
            add_note(notes, "Deep search was requested but no page content was read.")

    return _format_response(
        response,
        mode=mode,
        queries=queries,
        notes=notes,
        page_contents=page_contents,
    )
```

---

# 8. `_fetch_top_pages` 重命名为 `_read_page_contents`

删除旧名称：

```text
_fetch_top_pages
_include_page_content
```

统一叫：

```python
_read_page_contents
```

因为它的真实语义是：

```text
读取搜索结果 URL 对应页面的正文片段。
```

---

## 8.1 `_read_page_contents` 签名

```python
async def _read_page_contents(
    self,
    response: SearchResponse,
    *,
    session_id: str,
    limit: int,
    notes: Optional[List[str]] = None,
) -> List[str]:
```

---

## 8.2 受控并发读取页面正文

实现要求：

```text
1. 从 response.results 中按顺序取唯一 URL。
2. 使用 normalize_url_for_dedup 去重。
3. 最多读取 limit 个页面。
4. 使用 asyncio.Semaphore(PAGE_CONTENT_CONCURRENCY) 控制并发。
5. 单页 fetch / cache / format 失败只跳过当前页。
6. 返回内容顺序要和搜索结果顺序一致。
```

参考实现：

```python
async def _read_page_contents(
    self,
    response: SearchResponse,
    *,
    session_id: str,
    limit: int,
    notes: Optional[List[str]] = None,
) -> List[str]:
    candidates: List[Tuple[int, SearchResult]] = []
    seen: Set[str] = set()

    for result_index, result in enumerate(response.results, 1):
        url = result.url.strip()
        if not url:
            continue

        normalized_url = normalize_url_for_dedup(url)
        if not normalized_url or normalized_url in seen:
            continue

        seen.add(normalized_url)
        candidates.append((result_index, result))

        if len(candidates) >= limit:
            break

    semaphore = asyncio.Semaphore(PAGE_CONTENT_CONCURRENCY)

    async def read_one(result_index: int, result: SearchResult) -> Optional[Tuple[int, str]]:
        async with semaphore:
            return await self._read_one_page_content(
                result_index=result_index,
                result=result,
                response=response,
                session_id=session_id,
                notes=notes,
            )

    tasks = [
        read_one(result_index, result)
        for result_index, result in candidates
    ]

    raw_results = await asyncio.gather(*tasks, return_exceptions=True)

    contents: List[Tuple[int, str]] = []

    for item in raw_results:
        if isinstance(item, Exception):
            log_fail("页面正文读取任务失败", item)
            continue

        if item is None:
            continue

        contents.append(item)

    contents.sort(key=lambda item: item[0])

    return [content for _, content in contents]
```

---

## 8.3 `_read_one_page_content`

```python
async def _read_one_page_content(
    self,
    *,
    result_index: int,
    result: SearchResult,
    response: SearchResponse,
    session_id: str,
    notes: Optional[List[str]],
) -> Optional[Tuple[int, str]]:
    url = result.url.strip()

    try:
        content = await asyncio.wait_for(
            self._fetcher.fetch(url),
            timeout=PAGE_CONTENT_TIMEOUT_SECONDS,
        )
    except asyncio.TimeoutError as e:
        log_fail(
            "页面正文读取超时",
            e,
            url=url,
            timeout=PAGE_CONTENT_TIMEOUT_SECONDS,
        )
        add_note(
            notes,
            f"Page content for result #{result_index} was skipped because it timed out.",
        )
        return None
    except Exception as e:
        log_fail("页面正文读取失败", e, url=url)
        add_note(
            notes,
            f"Page content for result #{result_index} failed and was skipped.",
        )
        return None

    if content is None:
        add_note(
            notes,
            f"Page content for result #{result_index} returned no content and was skipped.",
        )
        return None

    if not isinstance(content, str):
        log_fail(
            "页面正文读取跳过",
            "Fetched content is not text Markdown, skip page content",
            url=url,
            content_type=type(content).__name__,
        )
        add_note(
            notes,
            f"Page content for result #{result_index} was skipped because it returned non-text content.",
        )
        return None

    content = content.strip()
    if not content:
        add_note(
            notes,
            f"Page content for result #{result_index} was empty and skipped.",
        )
        return None

    try:
        window = cache_and_window(
            session_id=session_id,
            tool_name=self.name,
            source=url,
            text=content,
            content_type="text/markdown",
            metadata={
                "query": response.query,
                "fetched_from": "web_search.deep",
            },
            offset=0,
            limit=PAGE_CONTENT_WINDOW_CHARS,
        )

        formatted_content = format_windowed_content(window)

    except Exception as e:
        log_fail(
            "页面正文窗口化失败",
            e,
            url=url,
            session_id=session_id,
        )
        add_note(
            notes,
            f"Page content for result #{result_index} failed during content processing and was skipped.",
        )
        return None

    text = (
        f"--- Page content for result #{result_index} ---\n"
        f"Title: {result.title.strip() or '(no title)'}\n"
        f"URL: {url}\n"
        "Content:\n"
        f"{formatted_content}"
    )

    return result_index, text
```

---

# 9. `_format_response` 修改

签名：

```python
def _format_response(
    response: SearchResponse,
    *,
    mode: str,
    queries: List[str],
    notes: Optional[List[str]] = None,
    page_contents: Optional[List[str]] = None,
) -> str:
```

输出标题：

```python
lines = ["[Tool Result] Web search evidence pack"]
lines.append(f"Mode: {mode}")
```

普通搜索：

```text
Mode: normal
```

Deep Search：

```text
Mode: deep
```

页面正文部分：

```python
if page_contents:
    lines.append("\nPage contents:")
    for content in page_contents:
        lines.append("")
        lines.append(content)
```

不要再输出：

```text
Fetched top pages
```

统一改为：

```text
Page contents
```

---

# 10. SearchCoordinator 重构

文件：

```text
chat/application/web_search/search_coordinator.py
```

---

## 10.1 `search_many` 成为唯一 Tool 搜索入口

签名保留简洁：

```python
async def search_many(
    self,
    queries: List[str],
    *,
    max_results_per_query: int = 8,
    final_max_results: int = 20,
    with_images: bool = False,
    concurrency: int = MAX_BROAD_SEARCH_CONCURRENCY,
    dedupe_domains: bool = DEFAULT_DEDUPE_DOMAINS,
    max_per_domain: int = DEFAULT_MAX_PER_DOMAIN,
    notes: Optional[List[str]] = None,
) -> SearchResponse:
```

---

## 10.2 `search_many` 内部永远不走 Tavily

内部调用：

```python
response = await self.search(
    query=query,
    max_results=max_results_per_query,
    with_images=with_images,
    allow_paid_fallback=False,
)
```

删除：

```text
allow_paid_fallback 参数
paid fallback 逻辑
tavily_called
tavily_used
PAID_FALLBACK_MIN_RESULTS
PAID_FALLBACK_LIMIT
```

---

## 10.3 删除 freshness_required

`search_many` 不再接收：

```text
freshness_required
```

内部调用 `search()` 时不传。
`search()` 自己可保留该参数作为底层能力，但 WebSearchTool 不用它。

---

## 10.4 删除 include/exclude domain filter

`search_many` 不再接收：

```text
include_domains
exclude_domains
```

`_merge_many_search_responses` 删除对应参数和逻辑。

---

## 10.5 删除 time_range

`search_many` 不再接收：

```text
time_range
```

调用：

```python
normalized_queries = normalize_queries(
    queries,
    limit=MAX_BROAD_SEARCH_QUERIES,
    notes=notes,
)
```

---

# 11. utils 清理

## 11.1 `utils/queries.py`

最终只保留：

```python
def normalize_queries(
    queries: List[str],
    *,
    limit: int,
    notes: Optional[List[str]] = None,
) -> List[str]:
    ...
```

删除：

```text
time_range
_append_current_year_if_needed
_YEAR_RE
_TIME_WORD_RE
_VALID_TIME_RANGES
```

保留：

```text
strip
空白折叠
去空
大小写去重
limit
400 字符截断
notes
```

---

## 11.2 `utils/domains.py`

保留：

```text
extract_domain
count_unique_domains
deduplicate_results_by_domain
has_site_operator
```

删除：

```text
filter_results_by_domains
_normalize_domain_list
```

---

## 11.3 `utils/urls.py`

保留：

```text
normalize_url_for_dedup
```

---

## 11.4 `utils/notes.py`

保留：

```text
add_note
```

---

## 11.5 `utils/params.py`

如果已经无调用，删除整个文件。

---

# 12. 测试大修

删除旧测试：

```text
query 单独参数测试
max_results 自定义值测试
final_max_results 自定义值测试
fetch_top_pages
fetch_top_pages_limit
include_page_content
page_content_limit
freshness_required
allow_paid_fallback
time_range
include_domains
exclude_domains
dedupe_domains 作为外部参数
max_per_domain 作为外部参数
参数严格校验
参数 clamp
normalize_int
normalize_bool
```

保留或新增测试：

```text
1. normal mode 使用 queries 并发搜索
2. deep mode 会读取页面正文
3. deep mode 默认读取最多 8 个唯一 URL
4. 页面正文读取使用 normalize_url_for_dedup 去重
5. 页面正文读取并发不会超过 5
6. 页面正文读取失败不影响其他页面
7. 页面正文读取输出绑定 result index
8. 非 str 内容跳过
9. None 内容跳过
10. cache_and_window 失败只跳过当前页
11. with_images 生效
12. site: 自动关闭 domain dedupe
13. website: / offsite: 不误触发 site:
14. WebSearchTool 永不触发 Tavily
15. search_many 永不触发 Tavily
16. URL normalize 保留 path 大小写
17. query normalize 去重 / 截断 / limit
18. Evidence Pack 输出包含 Mode / Queries / Source / Summary / Notes / Page contents
```

---

# 13. 最终行为示例

## 13.1 普通搜索

```json
{
  "queries": [
    "Python asyncio gather vs wait",
    "site:docs.python.org asyncio gather wait",
    "asyncio wait return_when FIRST_COMPLETED"
  ],
  "mode": "normal"
}
```

结果：

```text
并发搜索多个 query。
返回搜索结果摘要。
不读取页面正文。
不走 Tavily。
```

---

## 13.2 Deep Search

```json
{
  "queries": [
    "Docker compose network official docs",
    "Docker compose DNS service name",
    "Docker compose bridge network best practices"
  ],
  "mode": "deep"
}
```

结果：

```text
并发搜索多个 query。
合并去重。
读取 Top 8 个唯一 URL 的页面正文片段。
页面读取并发 5。
不走 Tavily。
```

---

## 13.3 图片搜索

```json
{
  "queries": ["埃菲尔铁塔 图片"],
  "mode": "normal",
  "with_images": true
}
```

---

# 14. Codex 执行提示词

```text
你是一个严谨的 Python 后端工程师。现在要对 web_search 做一次彻底重构：默认并发多搜，只保留 normal 和 deep 两种模式，删除高级参数和幽灵功能。不要新增功能，不改架构，不引入第三方库。

最终 Tool Schema 只保留：
- queries
- mode
- with_images

删除 Tool Schema 参数：
- query
- max_results
- final_max_results
- fetch_top_pages
- fetch_top_pages_limit
- include_page_content
- page_content_limit
- freshness_required
- fetch_page_timeout_seconds
- fetched_page_max_chars
- allow_paid_fallback
- dedupe_domains
- max_per_domain
- include_domains
- exclude_domains
- time_range

内部固定常量：
- SEARCH_QUERY_LIMIT = 4
- RESULTS_PER_QUERY = 8
- FINAL_RESULT_LIMIT = 20
- ALLOW_PAID_FALLBACK = False
- DEDUPE_DOMAINS = True
- MAX_PER_DOMAIN = 2
- PAGE_CONTENT_LIMIT = 8
- PAGE_CONTENT_CONCURRENCY = 5
- PAGE_CONTENT_TIMEOUT_SECONDS = 15.0
- PAGE_CONTENT_WINDOW_CHARS = 3000

WebSearchTool：
1. 删除 query 单参数入口，只接收 queries。
2. 删除单 query 精搜分支，所有请求统一走 SearchCoordinator.search_many。
3. mode 只允许 normal / deep，由 Tool Schema 约束。
4. mode=normal：只返回搜索结果摘要。
5. mode=deep：在搜索结果摘要之外，读取 Top 页面正文片段。
6. with_images 保留。
7. site: operator 自动关闭 domain dedupe。
8. WebSearchTool 调 search_many 时固定 allow_paid_fallback=False。
9. Tavily 不允许由 WebSearchTool 触发。
10. _fetch_top_pages / _include_page_content 统一重命名为 _read_page_contents。
11. 页面正文读取使用 asyncio.Semaphore(PAGE_CONTENT_CONCURRENCY) 受控并发。
12. 页面正文读取使用 normalize_url_for_dedup 去重。
13. 单页 fetch / cache_and_window / format_windowed_content 失败只跳过当前页。
14. 页面正文输出格式统一为：
    --- Page content for result #N ---
    Title:
    URL:
    Content:

SearchCoordinator：
1. search_many 删除参数：
   - freshness_required
   - allow_paid_fallback
   - include_domains
   - exclude_domains
   - time_range
2. search_many 内部调用 search(... allow_paid_fallback=False)。
3. 删除 search_many 内部 paid fallback 逻辑。
4. 删除 include/exclude domain filter。
5. normalize_queries 不再接收 time_range。
6. 保留 URL 去重、图片去重、domain dedupe、notes。

utils：
1. queries.py 删除 time_range / 年份补全相关逻辑。
2. domains.py 删除 include/exclude filter 相关函数。
3. urls.py 保留 normalize_url_for_dedup。
4. notes.py 保留 add_note。
5. params.py 如果无引用，删除。

测试：
1. 删除旧参数相关测试。
2. 新增/保留：
   - normal mode 多 query 搜索
   - deep mode 页面正文读取
   - deep mode 默认最多读取 8 个唯一 URL
   - 页面读取并发不超过 5
   - 单页失败不影响整体
   - cache_and_window 失败只跳过当前页
   - 页面正文输出绑定 result index
   - with_images 生效
   - site: 自动关闭 domain dedupe
   - website/offsite 不误触发 site:
   - WebSearchTool 不触发 Tavily
   - search_many 不触发 Tavily
   - Evidence Pack 输出包含 Page contents

运行：
- uv run python test/test_web_search_unit.py
- uv run python test/test_search_coordinator_failures.py

完成后报告：
1. 删除了哪些参数。
2. 删除了哪些函数。
3. 修改了哪些文件。
4. 是否保留 normal / deep 两种模式。
5. 是否引入新功能：必须为否。
```

---

# 15. 下一步 Review

这次大修完成后，下一步 review：

```text
web_search_tool.py
```

重点看：

```text
1. schema 是否只剩 queries / mode / with_images
2. execute 是否统一走 search_many
3. mode=deep 是否并发读取页面正文
4. Tavily 是否完全不触发
5. 是否没有旧参数残留
```

再下一步：

```text
search_coordinator.py
```

重点看：

```text
1. search_many 是否删除 paid fallback
2. 是否删除 include/exclude/time_range
3. 是否仍保持合并去重质量
```

---

这版文档的核心就是一句话：

```text
web_search 默认就是多 query 并发搜索；normal 返回摘要，deep 额外读取页面正文片段。
```
