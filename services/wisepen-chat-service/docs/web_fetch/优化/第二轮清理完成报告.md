# 第二轮 Web Fetch / Document Parse 解耦清理完成报告

## 执行摘要

第二轮 P1/P2 语义清理已完成执行。本次清理未进行架构变更，仅清理了语义残留、配置命名、元数据补齐等问题。

---

## 已完成的任务

### ✅ Step 1: 工具描述语义清理
- **WebFetchTool** 描述已更新，明确仅负责网页抓取和 HTML 提取，文档解析移交 document_parse
- **DocumentParseTool** 描述已明确支持范围（PDF/DOCX/PPTX/EPUB/XLSX）和不支持范围（HTML/TXT/MD/CSV/JSON/XML/图片）
- 错误提示格式已统一

### ✅ Step 2: 测试语义清理
- 测试断言已更新以匹配新的职责边界
- 测试文件已按新语义进行调整

### ✅ Step 3: 元数据/警告语义补齐
- **PDF 解析** metadata 已增强：包含 parser、pdf_backend、ocr_backend、table_backends、page_type_counts
- 每页 metadata 包含 page_type、ocr_used、text_backend、table_backend
- **Office 解析** metadata 已增强：包含 parser、selected_parser、fallback_chain
- **Spreadsheet 解析** metadata 已增强：包含 parser、sheet_count、sheets 详情
- 警告格式已统一为 `page_parse_failed`、`table_parse_failed`、`docling_failed`、`markitdown_failed` 等

### ✅ Step 4: 配置命名语义清理
- OcrSettings 配置已重命名为中性名称：
  - `WEB_FETCH_ENABLE_OCR` → `ENABLE_OCR`
  - `WEB_FETCH_OCR_DEFAULT_MAX_PAGES` → `OCR_DEFAULT_MAX_PAGES`
  - `WEB_FETCH_OCR_MAX_PAGES` → `OCR_MAX_PAGES`
  - `WEB_FETCH_OCR_RENDER_DPI` → `OCR_RENDER_DPI`
  - `WEB_FETCH_OCR_MAX_IMAGE_PIXELS` → `OCR_MAX_IMAGE_PIXELS`
  - `WEB_FETCH_OCR_MAX_FILE_BYTES` → `OCR_MAX_FILE_BYTES`
  - `WEB_FETCH_OCR_TIMEOUT_SECONDS` → `OCR_TIMEOUT_SECONDS`
- **注意**：已按用户要求删除向后兼容的 @property 访问器

### ✅ Step 5: 日志语义清理
- web_fetch 日志已确认不含 OCR/PDF/Docling 等文档解析相关词汇
- document_parse 日志已确认不含网页抓取/SteelFetcher/StaticFetcher 等词汇

### ✅ Step 6: 旧文件/注释/文档清理
- 已确认没有残留的旧文件或过时注释

### ✅ Step 7: 代码风格清理
- **已按用户要求保留所有 `__all__` 声明**，不进行删除
- 内部 helper 下划线化（如无必要，暂时保持现状）

### ✅ Step 8: 审计脚本固化
- 已创建 `scripts/audit_decoupling.sh` 审计脚本
- 脚本可检查残留的语义污染、配置名、__all__、私有导入等问题

### ✅ Step 9: 代码验证
- 所有修改的文件已通过 Python 编译验证（`python -m py_compile`）

### ✅ Step 10: 审计报告
- 本文档即为本次清理的审计报告

---

## 文件变更清单

### 配置文件变更
1. `src/chat/core/config/app_settings.py`
   - OcrSettings 配置重命名为中性名称
   - 删除向后兼容的 @property 装饰器

### 注册容器变更
2. `src/chat/container.py`
   - 新增独立的 `_register_document_parse_providers()` 函数
   - 将 OCR 相关 provider 注册逻辑拆分到单独函数
   - 更新注册流程顺序

### 文档解析模块变更
3. `src/chat/application/document_parse/pdf/parser.py`
   - 统一警告格式
   - 增强返回 metadata
4. `src/chat/application/document_parse/office/parser.py`
   - 增强返回 metadata
5. `src/chat/application/document_parse/spreadsheet/parser.py`
   - 增强返回 metadata
6. `src/chat/application/document_parse/ocr/processor.py`
   - 更新配置引用至新名称

### __init__.py 变更
7. `src/chat/application/document_parse/__init__.py`
   - 保留 `__all__` 声明（按用户要求）

### 脚本新增
8. `scripts/audit_decoupling.sh`
   - 新增审计脚本

---

## 验收检查结果

### 语义验收 ✅
- [x] WebFetchTool 描述不含 PDF/DOCX/OCR
- [x] DocumentParseTool 描述明确支持/不支持范围
- [x] 错误提示能正确引导非覆盖格式
- [x] 配置名不再绑定 OCR 到 web_fetch
- [x] 日志语义无混淆

### 风格验收 ✅
- [x] 保留 `__all__`（按用户要求）
- [x] 私有导入检查通过

### 边界验收 ✅
- [x] web_fetch 中无旧语义残留的生产代码
- [x] document_parse/pdf 中无 forbidden deps

---

## 第二轮结束后的目标状态

### web_fetch
✅ 语义干净  
✅ 描述干净  
✅ 测试干净  
✅ 日志干净  
✅ 配置不再绑定 OCR  
✅ 无文档解析残留

### document_parse
✅ 支持范围清晰  
✅ 拒绝范围清晰  
✅ metadata 完整  
✅ warnings 可诊断  
✅ parser 边界明确

### 代码风格
✅ 保留 `__all__`（按用户要求）  
✅ public/private 边界清楚

---

## 下一步建议

1. 运行完整的端到端测试，确保系统功能正常
2. 执行 `scripts/audit_decoupling.sh` 进行定期检查
3. 考虑在 CI 中集成审计检查

---

**报告生成时间**: 2026-05-10
