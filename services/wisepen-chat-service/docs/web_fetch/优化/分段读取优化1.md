下面是直接交给 Codex 的提示词。它把你刚才 3 个决策都纳入了：

```text
请修改 ToolContentStore 和相关 web_fetch/tool_content_read 接入逻辑，目标是把当前“简单 offset 字符截断”升级为更适合长文档的“结构化分块读取”。

背景：
- cachetools.TTLCache 支持 maxsize、ttl、getsizeof，可用于按内容大小管理缓存容量。
- 长文本分块不要只按固定字符硬切。社区常见做法是 recursive splitting：优先按自然边界切分，例如段落 "\n\n"，其次换行 "\n"，再其次空格 " "，最后才按字符硬切。
- LangChain 的 RecursiveCharacterTextSplitter 就是按分隔符列表递归切分文本；Pinecone 对 chunking 的总结也提到，常见默认分隔符是 ["\n\n", "\n", " ", ""]，用于尽量保留段落、句子和词边界。
- 这里不要引入 langchain-text-splitters。只实现一个轻量内部 chunker，避免新增重依赖。

参考实践依据：
- cachetools 的 cache 是固定最大尺寸的 mutable mapping，超过 maxsize 会按策略淘汰；TTLCache 支持 ttl 与 getsizeof。
- LangChain RecursiveCharacterTextSplitter 使用分隔符列表递归切分文本。
- 常见 recursive character splitting 会按 "\n\n"、"\n"、" "、"" 这样的优先级保留自然边界。

具体要求：

1. ToolContentStore 内部继续使用 cachetools.TTLCache。
   - 类内部字段继续使用下划线命名，例如 self._items。
   - 模块级导出继续使用 __all__。
   - 不要恢复 OrderedDict。
   - 不要手写 _evict_expired / _evict_overflow。

2. 缓存容量按字符总量限制，并从 settings 读取。
   - 在 settings 中增加：
     TOOL_CONTENT_STORE_TTL_SECONDS = 30 * 60
     TOOL_CONTENT_STORE_MAX_TOTAL_CHARS = 20_000_000
   - ToolContentStore.__init__ 默认参数从 settings 读取，或者在实例化 tool_content_store 时传入 settings。
   - TTLCache 使用：
     TTLCache(
         maxsize=max_total_chars,
         ttl=ttl_seconds,
         getsizeof=lambda item: len(item.text),
     )

3. 单个内容超过缓存总容量时静默处理，不要抛异常。
   - ToolContentStore.put(...) 返回 Optional[str]。
   - 如果 len(text) > self._items.maxsize：
       return None
   - 不要抛 ValueError。
   - WebFetchTool 如果 content_id is None，仍然返回当前内容的第一段/指定 offset 段，但是 metadata 必须明确告诉模型：
       content_cached: false
       cache_error: content_too_large
       truncated: true/false
       next_offset: 如果不能保证可续读，可以为空
       warning: Full content was too large to cache. Answer conservatively because later chunks may not be available from cache.
   - 模型必须知道：完整文件没有成功缓存，所以不能假设已经掌握全文。

4. 正常缓存成功时，metadata 必须包含：
   content_id
   content_cached: true
   tool_name
   source
   content_type
   original_length
   chunk_index
   chunk_count
   offset
   returned_length
   truncated
   next_offset

5. 引入“分块阅读”，不要再只用简单 offset + limit 字符硬切。
   实现一个内部轻量 chunker：
   - 优先按段落分隔 "\n\n"
   - 如果段落过大，再按行分隔 "\n"
   - 如果行仍过大，再按空格 " "
   - 如果仍过大，最后才按字符硬切
   - 每个 chunk 的目标最大长度为 limit
   - chunk 之间可以暂时不做 overlap，第一版保持简单
   - chunk 必须保持原文顺序
   - 不要丢内容
   - 不要重复大段内容

6. 推荐新增数据结构：
   @dataclass
   class ContentChunk:
       index: int
       start_offset: int
       end_offset: int
       text: str

   WindowedContent 可以增加：
       chunk_index: int
       chunk_count: int

7. StoredToolContent 建议增加 chunks 字段：
   chunks: List[ContentChunk] = field(default_factory=list)
   - put() 时根据 text 和默认 chunk_size 生成 chunks。
   - chunk_size 可以使用 settings.TOOL_RESULT_MAX_CHARS，或者 ToolContentStore 初始化参数 default_chunk_size。
   - 如果后续 read_window 传入 limit 与默认 chunk_size 不一致，可以第一版忽略动态重切，仍然使用已生成 chunks。
   - 更简单的第一版：read_window 仍接受 offset，但内部根据 offset 找到对应 chunk 返回。
   - 如果 offset 位于某个 chunk 中间，返回该 chunk 从 offset 开始的内容，或直接返回该 chunk 全文；建议返回该 chunk 全文并在 metadata 中给出 chunk_index。

8. tool_content_read 的参数保持：
   content_id
   offset
   limit
   不新增 chunk_index 参数，避免工具协议复杂化。
   但返回 metadata 中可以包含 chunk_index/chunk_count。
   模型仍然通过 next_offset 续读。

9. read_window 行为建议：
   - 根据 offset 找到第一个 end_offset > offset 的 chunk。
   - 返回该 chunk。
   - next_offset = chunk.end_offset if 不是最后一块 else None。
   - truncated = next_offset is not None。
   - offset >= original_length 时返回 error="offset_out_of_range"。
   - returned_length = len(chunk.text.strip())。
   - 这样 offset 仍然是统一续读协议，但实际返回的是自然边界 chunk，而不是机械字符窗口。

10. WebFetchTool 接入逻辑：
   - 抓取到完整 md_result 后，调用 tool_content_store.put(...)
   - 如果 content_id 成功：
       使用 tool_content_store.read_window(content_id, session_id, offset, limit)
       返回 ToolContent Metadata + Content
   - 如果 content_id 为 None：
       不要失败。
       使用本地 helper 对 md_result 做一次临时 chunk/window，返回当前片段。
       metadata 中明确 content_cached: false、cache_error: content_too_large。
       不要让模型误以为可以通过 tool_content_read 继续读取。
       如果还给 next_offset，必须提示“需要重新调用原工具且可能重新抓取”，不要提示 tool_content_read。

11. tool_content_read：
   - 只能读取缓存成功的 content_id。
   - 如果 content_id 不存在、过期、session 不匹配，返回：
     [Tool Result] Cached tool content not found, expired, or inaccessible.
   - 不要尝试重新抓取 URL。
   - 不要跨 session 读取。

12. 输出格式统一为 Markdown 文本，保持工具返回 str。
   推荐格式：

   [ToolContent Metadata]
   content_id: web_fetch:xxxx
   content_cached: true
   tool_name: web_fetch
   source: https://example.com/paper.pdf
   content_type: text/markdown
   original_length: 84321
   chunk_index: 0
   chunk_count: 8
   offset: 0
   returned_length: 11230
   truncated: true
   next_offset: 11230

   [Content]
   ...

   超大未缓存时：

   [ToolContent Metadata]
   content_id:
   content_cached: false
   cache_error: content_too_large
   tool_name: web_fetch
   source: https://example.com/huge.pdf
   content_type: text/markdown
   original_length: 120000000
   chunk_index: 0
   chunk_count:
   offset: 0
   returned_length: 11000
   truncated: true
   next_offset:
   warning: Full content was too large to cache. Answer conservatively because the complete document is not available from cache.

   [Content]
   ...

13. 不要做：
   - 不要引入 LangChain / LlamaIndex。
   - 不要引入 Redis / SQLite / 文件缓存。
   - 不要做向量检索。
   - 不要做 overlap。
   - 不要新增 chunk_index 工具参数。
   - 不要让超大内容缓存失败导致 web_fetch 整体失败。
   - 不要把未缓存内容伪装成已缓存内容。

验收标准：
1. ToolContentStore 使用 TTLCache + getsizeof 按字符总量限制。
2. 超过单项缓存上限时 put 返回 None，不抛异常。
3. WebFetchTool 在缓存失败时仍返回当前片段，但 metadata 明确 content_cached=false。
4. 正常长内容返回 chunk_index/chunk_count/next_offset。
5. tool_content_read 能用 content_id + offset 读取下一块。
6. 分块优先保留段落/换行/空格边界，而不是简单字符截断。
7. 不跨 session 读取缓存内容。
```

补充一下依据，方便你确认方向：`cachetools` 官方说明 cache 是固定最大尺寸的映射，超过容量会按策略淘汰，并支持 `TTLCache`、`getsizeof` 等机制；这适合你现在“按总字符数限制缓存”的场景。([Cachetools][1])
长文本分块方面，LangChain 的 recursive splitter 就是按一组分隔符递归切分文本；Pinecone 对 RAG chunking 的总结也提到常见顺序是 `"\n\n" → "\n" → " " → ""`，目标是在控制 chunk 大小的同时尽量保留段落、句子和词边界。([docs.langchain.com][2])

[1]: https://cachetools.readthedocs.io/?utm_source=chatgpt.com "cachetools - Read the Docs"
[2]: https://docs.langchain.com/oss/python/integrations/splitters/recursive_text_splitter?utm_source=chatgpt.com "Splitting recursively - Text splitter integration guide"
