可以，下面给你 **可直接落地的代码级方案**。
重点只动三类地方：

```text
1. protocol.py：允许 ACTION_FAILED 自定义 recovery_hint
2. actions/ref.py：click_ref 对 DOM detached 返回 refresh_snapshot；searchbox/combobox fill 后失效 refs
3. tool.py：引导 focused snapshot 和搜索框填写后的下一步
```

你现在 `snapshot.py` 已经有 `resolve_current()`，并且 snapshot tree 已经改成 `ref=e3 | role=searchbox | ...` 这种格式，所以这里不再动 snapshot 核心。
`tool.py` 里也已经有 ref pattern 和 ref 规则描述，可以在这个基础上补几句。

---

# 1. `protocol.py`：给 `make_action_failed_error` 加可选 recovery_hint

当前 `make_action_failed_error()` 默认都是：

```text
inspect_page_state
```

但 `element detached / not stable` 这类 click 失败，本质上应该是：

```text
refresh_snapshot
```

改成这样：

```python
def make_action_failed_error(
    *,
    action_type: str,
    message: str,
    diagnostic_code: str,
    retryable: bool = False,
    context: Optional[Dict[str, Any]] = None,
    recovery_hint: Optional[RecoveryHint] = None,
) -> ToolError:
    return ToolError(
        category=ErrorCategory.ACTION.value,
        code=ToolErrorCode.ACTION_FAILED.value,
        message=message,
        retryable=retryable,
        diagnostic_code=diagnostic_code,
        context={
            "action_type": action_type,
            **(context or {}),
        },
        recovery_hint=recovery_hint or RecoveryHint(
            type="inspect_page_state",
            required_before_retry=False,
            reason="The page may still be usable. Inspect current state before retrying.",
        ),
    )
```

这样不新增错误码，也不新增新工厂。普通 action failed 行为不变，只是允许个别 action 覆盖 recovery hint。

---

# 2. `actions/ref.py`：click_ref 识别 detached/stale DOM

加这几个辅助函数：

```python
DETACHED_DOM_ERROR_PATTERNS = (
    "element is not attached to the dom",
    "element is not attached",
    "not attached to the dom",
    "detached from document",
    "element is not stable",
    "element is not visible",
)


def is_detached_dom_error(error: Exception) -> bool:
    message = str(error).lower()
    return any(pattern in message for pattern in DETACHED_DOM_ERROR_PATTERNS)
```

然后在 `handle_click_ref` 的 `except` 里改成这种结构：

```python
except Exception as error:
    log_fail("浏览器点击", str(error))

    page_state = await get_page_state(page)

    if is_detached_dom_error(error):
        return build_error_response(
            session_state=session_state(session_manager),
            page_state=page_state,
            error=make_action_failed_error(
                action_type="click_ref",
                message="Click target changed in the DOM. Take a fresh snapshot before retrying.",
                diagnostic_code="CLICK_TARGET_DETACHED",
                retryable=True,
                context={
                    "ref": ref,
                    "snapshot_id": resolved_snapshot_id,
                },
                recovery_hint=RecoveryHint(
                    type="refresh_snapshot",
                    required_before_retry=True,
                    reason="The target element changed, detached, or became unstable. Take a new snapshot before retrying.",
                ),
            ),
        )

    return build_error_response(
        session_state=session_state(session_manager),
        page_state=page_state,
        error=make_action_failed_error(
            action_type="click_ref",
            message="Click failed.",
            diagnostic_code="CLICK_FAILED",
            context={
                "ref": ref,
                "snapshot_id": resolved_snapshot_id,
            },
        ),
    )
```

对应 import 需要补：

```python
from ..protocol import (
    ...
    RecoveryHint,
    ...
)
```

这个修的是你 Wikipedia 那种情况：
`fill_ref e3` 后搜索框触发 autocomplete，旧的 `e4 Search` 可能已经 detached / unstable；此时返回 `refresh_snapshot` 比普通 `ACTION_FAILED` 更准确。

---

# 3. `actions/ref.py`：搜索框/combobox 填写后 invalidate snapshot

这条更直接：
如果填的是 `searchbox / combobox / input[type=search] / autocomplete`，填完后 DOM 大概率会变，旧 refs 不应该继续有效。

加辅助函数：

```python
async def should_invalidate_after_fill(target) -> bool:
    try:
        data = await target.evaluate(
            """el => ({
                tag: el.tagName.toLowerCase(),
                role: (el.getAttribute('role') || '').toLowerCase(),
                type: (el.getAttribute('type') || '').toLowerCase(),
                ariaAutocomplete: (el.getAttribute('aria-autocomplete') || '').toLowerCase(),
                ariaExpanded: (el.getAttribute('aria-expanded') || '').toLowerCase(),
                hasList: Boolean(el.getAttribute('list'))
            })"""
        )
    except Exception:
        return False

    role = data.get("role")
    input_type = data.get("type")
    aria_autocomplete = data.get("ariaAutocomplete")
    aria_expanded = data.get("ariaExpanded")

    return (
        role in {"searchbox", "combobox"}
        or input_type == "search"
        or aria_autocomplete in {"list", "both", "inline"}
        or aria_expanded == "true"
        or bool(data.get("hasList"))
    )
```

然后在 `handle_fill_ref` 里，填充前先判断：

```python
refs_invalidated = await should_invalidate_after_fill(fill_target)

await fill_target.fill(text)

if refs_invalidated:
    snapshot_manager.invalidate()
```

成功响应建议这样写：

```python
detail = {
    "ref": ref,
    "snapshot_id": resolved_snapshot_id,
    "text_length": len(text),
}

if refs_invalidated:
    detail["refs_invalidated"] = True
```

最终：

```python
return build_success_response(
    session_state=session_state(session_manager, reused=True),
    page_state=page_state,
    action_result=ActionResult(
        type="fill_ref",
        status="completed",
        detail=detail,
    ),
)
```

这个字段不是调试字段，它表达的是工具状态：**这次 fill 后旧 refs 已失效**。
这样模型下一步再点旧 ref，会收到 `SNAPSHOT_REQUIRED`；更理想的是它看到 `refs_invalidated=true` 后直接 snapshot 或按 Enter。

---

# 4. `tool.py`：强化搜索框和 focused snapshot 引导

你当前 `TOOL_DESCRIPTION` 已经不错，但可以加两句关键规则。改成类似：

```python
TOOL_DESCRIPTION = (
    "Agent-safe browser interaction tool based on snapshot+ref. "
    "Operates a local persistent browser with dedicated automation profile. "
    "Each call performs ONE action. Do not issue multiple browse_interact calls in parallel; "
    "wait for each result before deciding the next action. "
    "Refs do not exist until you call snapshot. "
    "After snapshot, use only exact refs from snapshot output, such as e5. "
    "Never use role names or labels as ref values, such as searchbox, button, link, textbox, Search, or Sign in. "
    "Flow: 1) navigate, 2) snapshot, 3) use click_ref/fill_ref/select_ref/check_ref with exact ref ids. "
    "snapshot_id is optional for ref actions and defaults to the current snapshot. "
    "Always pass browser_session_id from the previous response. "
    "Use fill_ref only on refs with flags=fillable. "
    "Use click_ref for links and buttons. "
    "After filling a searchbox or combobox, press Enter with key or take a fresh snapshot before clicking suggestions/buttons. "
    "For finding a specific control on a complex page, prefer snapshot with mode='focused' and a goal such as 'search box' or 'sign in'. "
    "For login pages, click the sign-in link first if the current page only shows a sign-in link. "
    "On STALE_REF or SNAPSHOT_REQUIRED, take a new snapshot first. "
    "On USER_INTERVENTION_REQUIRED, prompt the user to act in the browser window."
)
```

再改 schema 里的 snapshot 描述：

```python
"mode": {
    "type": "string",
    "enum": ["full", "focused"],
    "description": (
        "Snapshot mode. Default: full. "
        "Use focused when looking for a specific control or target on a complex page."
    ),
},
"goal": {
    "type": "string",
    "description": (
        "Focused snapshot ranking hint, such as 'search box', 'sign in', "
        "'password field', 'submit button', or 'content area'."
    ),
},
"limit": {
    "type": "integer",
    "minimum": 1,
    "maximum": 200,
    "description": "Maximum elements returned by focused snapshot.",
},
```

再把 action description 里的 snapshot 行改成：

```python
"- snapshot: mode/goal/limit are optional; use mode='focused' with goal to find a specific control\n"
```

---

# 5. 最小效果预期

改完后，Wikipedia 搜索链路会变成更稳定的两种之一。

## 路径 A：fill 搜索框后按 Enter

```json
{"type": "fill_ref", "ref": "e3", "text": "apple"}
```

返回：

```json
{
  "refs_invalidated": true
}
```

然后模型更倾向：

```json
{"type": "key", "text": "Enter"}
```

## 路径 B：fill 搜索框后重新 snapshot

```json
{"type": "snapshot"}
```

新 snapshot 看到：

```text
ref=e4 | role=option | label="Apple"
ref=e15 | role=button | label="Search"
```

然后点击新的 ref，不再点旧 `e4`。

---

# 6. 如果只想先做最小改动

最小集是：

```text
1. protocol.py 给 make_action_failed_error 加 recovery_hint 参数。
2. ref.py click_ref detached 错误返回 refresh_snapshot。
3. ref.py searchbox/combobox fill 后 snapshot_manager.invalidate()。
4. tool.py 加两句描述：
   - 不要并行调用 browse_interact。
   - searchbox/combobox fill 后按 Enter 或重新 snapshot。
```

不用动 schema 分支，不用加新错误码，不用重构。
